# my-first-prototype

Testing prototyping workflow.

**Next.js app:** `prototype/` — B2C car insurance client zone. See `helper-docs/01-context.md`, `02-structure.md`, `03-startup.md`.

**Run locally:** `cd prototype && npm install && npm run dev` (requires [Node.js LTS](https://nodejs.org)).
